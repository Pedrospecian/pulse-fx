{
  "name": "@pulse-fx/core",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "pretest": "cd ../db && npm run generate && npm run build",
    "test": "vitest run",
    "test:watch": "npm run pretest && vitest"
  },
  "dependencies": {
    "@pulse-fx/db": "*"
  },
  "devDependencies": {
    "typescript": "^5.5.3",
    "vitest": "^2.0.3"
  }
}
